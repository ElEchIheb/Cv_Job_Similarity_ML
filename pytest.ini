[pytest]
addopts = -p no:cacheprovider --basetemp=tests_tmp/pytest
testpaths = tests
# The Streamlit app (frontend/) is deprecated — superseded by web/ (Next.js) —
# and is intentionally excluded from routine test collection.
norecursedirs = frontend web node_modules .venv
